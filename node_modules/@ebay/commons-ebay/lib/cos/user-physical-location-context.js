'use strict';

const { LocationContext } = require('./location');

class UserPhysicalLocationContext extends LocationContext {
    constructor(multiValue, req) {
        super(multiValue, req);  
    }
}

module.exports.create = function(multiValue, req) {
    return new UserPhysicalLocationContext(multiValue, req);
};
