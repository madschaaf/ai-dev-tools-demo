'use strict';

function defineCons(name, constValue) {
    Object.defineProperty(module.exports, name, {
        value: constValue,
        enumerable: true
    });
}

function defineConsObj(obj) {
    for (var name in obj) {
        if (obj.hasOwnProperty(name)) {
            defineCons(name, obj[name]);
        }
    }
}

var con = {
    'BEARER': 'Bearer ',
    'hostedAppTokenExpiry': 72001,
    'ajaxTokenExpiry': 72001,
    'renewDelayOnFailure': 60000,
    'renewTimeBeforeExpiry': 600,
    'publicTokenExpiry': 1296000,
    'PUBLIC_SCOPE': 'https://api.ebay.com/oauth/scope/@public'
};

defineConsObj(con);
