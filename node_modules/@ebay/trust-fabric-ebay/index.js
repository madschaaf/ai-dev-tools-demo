'use strict';

const tokenGenerator = require('./lib/TokenGenerator');

// Pre-fetch the token
tokenGenerator.getToken((err, token) => {
    // no op
});

module.exports = tokenGenerator;
module.exports.TokenGenerator = tokenGenerator.TokenGenerator;
