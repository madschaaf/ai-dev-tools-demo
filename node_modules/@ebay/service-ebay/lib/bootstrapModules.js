'use strict';
const packageJson = require('../package.json');

class bootstrapModules {
    static getModules() {
        const modules = packageJson.bootstrap;
        const bootstrapModuleKeys = Object.keys(modules);

        // Return the keys as an array
        return bootstrapModuleKeys;
    }
}

module.exports = bootstrapModules;
