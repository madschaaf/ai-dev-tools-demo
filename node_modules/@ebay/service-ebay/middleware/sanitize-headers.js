'use strict';

const onHeaders = require('on-headers');

module.exports = function () {
    return (req, res, next) => {
        onHeaders(res, () => {
            res.removeHeader('X-Powered-By');
        });
        next();
    };
};
