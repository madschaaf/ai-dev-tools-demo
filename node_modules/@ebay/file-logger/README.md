# File-logger

File logger will log JSON data to file in logs folder.

## Install

```shell
    $ npm install file-logger
```

## Usage

```javascript
    const Logger = require('file-logger');

    
    const logger = new Logger('mylogfilename');
    logger.log({key: value});
```