'use strict';
function UseNode(props) {
    UseNode.$super.call(this, 'i18n-use');
    if (props) {
        this.setProperties(props);
    }
}

UseNode.nodeType = 'element';

UseNode.prototype = {
    doGenerateCode: function (template) {
        // This is a dummy instance that will get removed by the transform
        throw new Error('Node should not be in tree after transform');
    }
};

module.exports = UseNode;