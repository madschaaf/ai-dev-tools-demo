import getPreloadScript from "../../lib/get-preloaded-script";
// Below we serialize all preloaded bundles to the browser in a
// script that gets combined with the scripts Marko flushes.

$ const preloadScript = getPreloadScript(out);
$ if (preloadScript) {
    out.script(preloadScript);
}
