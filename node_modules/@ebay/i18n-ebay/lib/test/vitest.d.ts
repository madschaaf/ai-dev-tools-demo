import type { VitestUtils } from "vitest";
import ContentManager from "../ContentManager";
import { createI18NBundlePreloader } from "./common";

export declare const setup: () => { contentManager: ContentManager };

declare module "vitest" {
  interface VitestUtils {
    preloadI18nBundles: ReturnType<typeof createI18NBundlePreloader>;
  }
}
