'use strict';

const RuntimeError = module.exports.RuntimeError = require('@ebay/errors-ebay/errors').RuntimeError;

const SecurityError = module.exports.SecurityError = function SecurityError() {
    RuntimeError.apply(this, arguments);
};

require('util').inherits(SecurityError, RuntimeError);

/*
    Types of errors:
        invalid_token,
        bad_scope,
        missing_token

*/
