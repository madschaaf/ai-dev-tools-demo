'use strict';

const LRU = require('lru-cache');

const cache = new LRU(10000);

module.exports.set = function set(key, value) {
    cache.set(key, value);
};

module.exports.get = function get(key) {
    return cache.get(key);
};

module.exports.reset = cache.reset.bind(cache);
