import { IncomingMessage } from 'http';

export class Response extends IncomingMessage {
    body: string | object | Buffer | undefined;
}

declare global {
    namespace ServiceInstruments {
        interface ContextualClient {
            query(query: string, variables?: any, options?: any): Promise<Response>;
        }
    }
}
