import * as env from '..';

// $ExpectType boolean
env.isDev() && env.isTest() && env.isLive() && env.isLnp() &&
    env.isPreProd() && env.isProd() && env.isQA() &&
    env.isSandbox() && env.isSecure() && env.isStage();

// $ExpectType boolean
env.isNotDev() || env.isNotLive() || env.isNotLnp() ||
    env.isNotPreProd() || env.isNotProd() || env.isNotQA() ||
    env.isNotSandbox() || env.isNotStage() || env.isNotTest();

// $ExpectError
env.isDev();
