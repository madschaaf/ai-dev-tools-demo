import client from '@ebay/fidelius-client-ebay';

const headers = {
    'Content-Type': 'application/json',
};

client.deleteSecret('/path/to/secret', headers, (error, result, body) => {});
client.setSecret('/path/to/secret', 'secretValue', headers, (error, result, body) => {});
client.getSecret('/path/to/secret', headers, (error, result, body) => {});

client.setAclNode('/path/to/acl', { read: true, write: false }, headers, (error, result, body) => {});
client.getAclNode('/path/to/acl', headers, (error, result, body) => {});

// @ts-expect-error
client._makeRequest('/path/to/secret', headers, { method: 'GET' }, (error, result, body) => {}, 'service-name');
