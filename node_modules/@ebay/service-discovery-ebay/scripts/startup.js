'use strict';

require('../lib/index')
    .refreshCache()
    .then(() => {
        console.log('Successfully created service-discovery json file at startup ');       /* eslint-disable-line */
        process.exit(0);
    })
    .catch(e => {
        console.error('Unable to create service-discovery json file at startup', e);
        process.exit(1);
    });
