import { SpanProcessor, ReadableSpan } from '@opentelemetry/sdk-trace-node';
import { Span, Context } from '@opentelemetry/api';
import { FeatureFlagErrorInfo } from '../types/FeatureFlagErrorInfo';
export declare class FeatureFlagSpanProcessor implements SpanProcessor {
    private traceIdMap;
    constructor();
    /** Do nothing. For satisfying SpanProcessor interface only */
    onStart(span: Span, parentContext: Context): void;
    onEnd(span: ReadableSpan): void;
    /** Do nothing. For satisfying SpanProcessor interface only */
    shutdown(): Promise<void>;
    /** Do nothing. For satisfying SpanProcessor interface only */
    forceFlush(): Promise<void>;
    private report;
    private getFeatureFlagErrorInfo;
    /**
     * Return the error message name from span
     */
    private getErrorName;
    /**
     * Returns the span status which is defined by SpanStatusCode
     * 0 = UNSET
     * 1 = OK
     * 2 = ERROR
     * @param span
     * @returns
     */
    private getStatus;
    private getHttpStatusCode;
    removeTrace(traceId: string): void;
    removeTraceMap(): void;
    getTraceIdMap(): Map<string, FeatureFlagErrorInfo>;
}
