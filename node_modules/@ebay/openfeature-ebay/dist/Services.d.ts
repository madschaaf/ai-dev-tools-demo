import { EvaluationContext } from '@openfeature/js-sdk';
import { EvaluationResults, EvaluationResultsMap } from './types/FlagEvalDetail';
/**
 * This class is used for handling all services that may be used in the middleware.
 * Currently it is calling epsvc and it does not require any authentication (hence empty req)
 */
export default class Services {
    client: any;
    constructor();
    invokeInternalSearchApiByFlagKey(evalContext: EvaluationContext, flagKey: string): Promise<EvaluationResultsMap>;
    invokeInternalSearchApiByFlagList(evalContext: EvaluationContext, flagsList: Set<string>): Promise<EvaluationResultsMap>;
    invokeInternalSearchApiByLabel(evalContext: EvaluationContext, labelForEval: Set<string>): Promise<EvaluationResultsMap>;
    private call;
}
export declare function convertToEvaluationResultsMap(evaluationResults: EvaluationResults): {
    evaluationResultsMap: Map<any, any>;
    epTagsEvent: string;
};
