"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * A hook that is used to set the EvaluationContext with required values
 * from request before flag evaluation.
 */
class QualificationCriteriaHook {
    before(hookContext, hookHints) {
        const evalContext = hookContext.context;
        if (hookHints) {
            Object.entries(hookHints).forEach(([key, value]) => {
                if (typeof value === 'string' || typeof value === 'number') {
                    evalContext[key] = value;
                }
            });
        }
        return evalContext;
    }
}
exports.default = QualificationCriteriaHook;
//# sourceMappingURL=QualificationCriteriaHook.js.map