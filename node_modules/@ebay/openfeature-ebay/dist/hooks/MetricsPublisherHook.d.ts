import { EvaluationDetails, FlagValue, Hook, HookContext } from '@openfeature/js-sdk';
import { MetricsPublisher } from '../metrics/MetricsPublisher';
import { LogTransaction, Logger } from '@ebay/logging-inc';
export declare class MetricsPublisherHook implements Hook {
    private readonly metricsPublisher;
    startTimeInNanoSeconds: bigint;
    transaction: LogTransaction;
    logger: Logger;
    constructor(metricsPublisher: MetricsPublisher);
    before(): void;
    after(hookContext: HookContext, evaluationDetails: EvaluationDetails<FlagValue>): void;
    finally(): void;
}
