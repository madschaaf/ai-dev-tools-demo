export declare class FeatureFlagErrorInfo {
    private errorEvents;
    private errorsLogged;
    constructor();
    /**
     * Counts up the total count of errors from each error name in errorEvents.
     * @returns
     */
    getTotalErrorCount(): number;
    addErrorEvent(name: string): void;
    getErrorsList(): string[];
    getErrorEvents(): Map<string, number>;
    private countOne;
}
