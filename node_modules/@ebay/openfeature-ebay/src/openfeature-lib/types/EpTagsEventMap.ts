type EPTags = {
    qTags: EPTag[];
};

type EPTag = {
    key: string;
    value: string;
    type: TrackingTagTypes;
};

enum TrackingTagTypes {
    SCALAR = 'SCALAR',
    VECTOR = 'VECTOR',
}

export { EPTag, EPTags, TrackingTagTypes };
