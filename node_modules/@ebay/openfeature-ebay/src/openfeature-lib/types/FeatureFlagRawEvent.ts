import { FeatureFlagCalEvent } from './FeatureFlagCalEvent';

/**
 * TypeScript interface for the Java class:
 * com.ebay.ep.diagnostics.cal.FeatureFlagRawEvent
 * 
 * Raw event not for JSON serialization in CAL log.
 * This interface represents the structure of raw feature flag events
 * used for internal processing before CAL logging.
 */
interface FeatureFlagRawEvent {
    /**
     * Map where key is error type name, value is counter
     */
    calErrorCountMap?: Record<string, number>;

    /**
     * The CAL event data associated with this raw event
     */
    calEvent?: FeatureFlagCalEvent;
}

export { FeatureFlagRawEvent };
