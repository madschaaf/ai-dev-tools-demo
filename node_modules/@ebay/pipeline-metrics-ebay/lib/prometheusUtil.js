const { platformMetrics } = require('@ebay/prometheus-ebay');

const gTransactionTime = platformMetrics.createGauge(
    'api_calls_elapsed_time',
    'Time taken to execute the API request in ms',
    ['method', 'status', '_namespace_']
);

module.exports = {
    gTransactionTime
};
