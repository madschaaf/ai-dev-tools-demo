'use strict';

const logger = require('@ebay/logging-inc').logger('@ebay/pipeline-metrics-ebay');

module.exports = function starttime() {
    return function startTime(req, res, next) {
        req.startTime = Date.now();
        logger.info('Request startTime in millisecond : %dms', req.startTime);
        next();
    };
};
