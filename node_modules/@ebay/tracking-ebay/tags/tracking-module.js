'use strict';
var util = require('./util');

module.exports = {
    render: function render(input, context) {
        var req = util.getRequest(context);
        var moduleId = input.id;
        req.ebay.tracking.trackModuleImpression(moduleId);
    }
};
