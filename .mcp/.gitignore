# Ignore actual configuration files that may contain sensitive tokens
config.json
cline_mcp_settings.json
claude_desktop_config.json

# Keep template and example files
!*.template.json
!*.example.json
!*.example

# Keep documentation
!*.md

# Keep backups directory structure but ignore contents
backups/*
!backups/.gitkeep
